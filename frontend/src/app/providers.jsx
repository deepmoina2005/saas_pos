import React from 'react';

/**
 * Global App Providers
 * Wraps the app in Redux, React Query, and Router providers
 */
export default function AppProviders({ children }) {
  return <>{children}</>;
}
